To run this example, run with the run.sh script, which is just a simple script that requires the path to the Rosetta discovery executable and the flags file. Run information is in the comments of the file.

For the run to be successful, you need access to a library of motifs (the standard library is too large to share in this repository).
